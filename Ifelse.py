# We have declared a value called name which take in user input.
name = raw_input("Name ?")

# The if Name matches with any of condition set it would return the Name that we have enterted if not will return a not a 
# Correct Name
if name == "mark":
	print("the name entered is", name)
elif name == "john":
	print("The name entered is", name)
else:
	print("the name entered is not valid the name entred is", name)
